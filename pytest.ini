[pytest]
testpaths = tests
addopts = -q
# Put the repo root on sys.path so `pytest` (the bare command in the README)
# can import `src`. Without this only `python -m pytest` works, because that
# form implicitly prepends the working directory.
pythonpath = .
